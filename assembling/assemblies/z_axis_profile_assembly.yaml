ShellforgepyBuilderVersion: "2026-03-27"

Description: Assembly configuration for a single z-axis profile

Builder:
  Visualization:
    parts:
      - source: self
        artifact: leader
        name: z_axis_profile
  Production:
    parts: []

Parameters:
  side:
    Type: String
  z_axis_profile_length:
    Type: Float

Parts:
  ZAxisProfileAssembly:
    Type: Shellforgepy::Assembly

    Properties:
      Generator: mege_ender_3v3ke_idex.designs.assemblies.z_axis_profile_assembly.create_z_axis_profile_assembly
      Properties:
        side:
          $ref: side
        z_axis_profile_length:
          $ref: z_axis_profile_length
