ShellforgepyBuilderVersion: "2026-03-27"

Description: Assembly configuration for one z-axis guide/threaded rod pair

Builder:
  Visualization:
    parts:
      - source: self
        artifact: leader
        name: guide_rod
      - source: self
        artifact: non_production_parts
        name_template: "{name}"
  Production:
    parts: []

Parameters:
  z_axis_base_z_offset:
    Type: Float

Parts:
  ZAxisRodsAssembly:
    Type: Shellforgepy::Assembly

    Properties:
      Generator: mege_ender_3v3ke_idex.designs.assemblies.z_axis_rods_assembly.create_z_axis_rods_assembly
      Properties:
        z_axis_base_z_offset:
          $ref: z_axis_base_z_offset
