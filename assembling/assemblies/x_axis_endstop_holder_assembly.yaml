ShellforgepyBuilderVersion: "2026-03-27"

Description: Assembly configuration for one generic endstop holder

Builder:
  Visualization:
    parts:
      - source: self
        artifact: leader
        name: endstop_holder
      - source: self
        artifact: non_production_parts
        name_template: "{name}"
  Production:
    parts:
      - source: self
        artifact: leader
        name: endstop_holder

Parts:
  XAxisEndstopHolderAssembly:
    Type: Shellforgepy::Assembly

    Properties:
      Generator: mege_ender_3v3ke_idex.designs.assemblies.y_axis_endstop_holder_assembly.create_y_axis_endstop_holder_assembly
      Properties: {}
