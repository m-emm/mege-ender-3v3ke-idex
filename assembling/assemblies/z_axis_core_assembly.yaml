ShellforgepyBuilderVersion: "2026-03-27"

Description: Assembly configuration for a single z-axis core

Builder:
  Visualization:
    parts:
      - source: self
        artifact: followers
        name_template: "{name}"
      - source: self
        artifact: non_production_parts
        name_template: "{name}"
  Production:
    parts:
      - source: self
        artifact: followers
        name_template: "{name}"

Parameters:
  side:
    Type: String

Parts:
  ZAxisCoreAssembly:
    Type: Shellforgepy::Assembly

    Properties:
      Generator: mege_ender_3v3ke_idex.designs.assemblies.z_axis_core_assembly.create_z_axis_core_assembly
      Properties:
        side:
          $ref: side
