ShellforgepyBuilderVersion: "2026-03-27"

Description: Assembly configuration for one standalone x-axis endstop

Builder:
  Visualization:
    parts:
      - source: dependencies
        assembly: x_axis_lower_profile_assembly
        artifact: leader
        name: x_axis_lower_profile
      - source: dependencies
        assembly: x_axis_rail_assembly
        artifact: leader
        name: x_axis_rail
      - source: self
        artifact: leader
        name: x_axis_endstop
      - source: self
        artifact: non_production_parts
        name_template: "{name}"
  Production:
    parts:
      - source: self
        artifact: leader
        name: x_axis_endstop

Parameters:
  x_axis_endstop_mount_base_depth:
    Type: Float
  x_axis_endstop_mount_base_thickness:
    Type: Float
  x_axis_endstop_mount_base_fillet_radius:
    Type: Float
  x_axis_endstop_mount_base_width:
    Type: Float
  x_axis_endstop_mount_base_inward_extension:
    Type: Float
  endstop_holder_mount_screw_size:
    Type: String
  endstop_holder_mount_screw_length:
    Type: Float
  endstop_holder_mount_plate_width:
    Type: Float
  endstop_holder_groove_holder_bottom_width:
    Type: Float
  endstop_holder_groove_holder_top_width:
    Type: Float
  endstop_holder_groove_holder_height:
    Type: Float
  endstop_holder_groove_holder_slit:
    Type: Float

Parts:
  XAxisEndstopAssembly:
    Type: Shellforgepy::Assembly

    Properties:
      Generator: mege_ender_3v3ke_idex.designs.assemblies.x_axis_endstop_assembly.create_x_axis_endstop_assembly
      Properties:
        x_axis_endstop_mount_base_depth:
          $ref: x_axis_endstop_mount_base_depth
        x_axis_endstop_mount_base_thickness:
          $ref: x_axis_endstop_mount_base_thickness
        x_axis_endstop_mount_base_fillet_radius:
          $ref: x_axis_endstop_mount_base_fillet_radius
        x_axis_endstop_mount_base_width:
          $ref: x_axis_endstop_mount_base_width
        x_axis_endstop_mount_base_inward_extension:
          $ref: x_axis_endstop_mount_base_inward_extension
        endstop_holder_mount_screw_size:
          $ref: endstop_holder_mount_screw_size
        endstop_holder_mount_screw_length:
          $ref: endstop_holder_mount_screw_length
        endstop_holder_mount_plate_width:
          $ref: endstop_holder_mount_plate_width
        endstop_holder_groove_holder_bottom_width:
          $ref: endstop_holder_groove_holder_bottom_width
        endstop_holder_groove_holder_top_width:
          $ref: endstop_holder_groove_holder_top_width
        endstop_holder_groove_holder_height:
          $ref: endstop_holder_groove_holder_height
        endstop_holder_groove_holder_slit:
          $ref: endstop_holder_groove_holder_slit